# duanxianxia 用户约定（固定执行版）

最后更新：2026-04-10 UTC

这个文件记录本项目已经被用户明确确认过的**固定要求**。后续回答、抓取、落盘、出表，默认都按这里执行，不再靠临时回忆。

## 1. 数据集命名与口径锁定

后续一律按 **`dataset_id + source_path`** 锁定，不再按中文名猜测。

| dataset_id | 中文名称 | source_path | 说明 |
|---|---|---|---|
| `rank.rocket` | 飙升榜 | 飙升榜 | 已确认 |
| `pool.surge` | 冲涨 | 股票池/冲涨 | 已确认 |
| `pool.hot` | 热门 | 股票池/热门 | 已确认；不得再和热度榜混淆 |
| `rank.hot_stock_day` / `hot_stock_day` | 热度榜（日） | 热度榜（日） | 与 `pool.hot` 严格区分 |
| `review.daily.top_metrics` | 每日复盘顶部指标 | 复盘/每日复盘 | 已确认 |
| `review.ltgd.range` | 龙头高度区间涨幅 | 复盘/龙头高度 | 已确认 |
| `auction.jjyd.vratio` | 竞价爆量 | 竞价异动/爆量 | 已确认 |
| `auction.jjyd.qiangchou` | 竞价抢筹 | 竞价异动/抢筹 | 已确认 |
| `auction.jjyd.net_amount` | 竞价净额 | 竞价异动/净额 | 已确认 |
| `auction.jjlive.fengdan` | 竞价封单 | 竞价封单 | 已确认 |
| `cashflow.stock.today` | 个股资金流向/今日排行 | 资金流向/个股资金流向/今日排行 | 已确认 |
| `cashflow.stock.3day` | 个股资金流向/3日排行 | 资金流向/个股资金流向/3日排行 | 已确认 |
| `cashflow.stock.5day` | 个股资金流向/5日排行 | 资金流向/个股资金流向/5日排行 | 已确认 |
| `cashflow.stock.10day` | 个股资金流向/10日排行 | 资金流向/个股资金流向/10日排行 | 已确认 |

### 强制规则
- `pool.hot` = **热门**，不是热度榜（日）。
- `rank.hot_stock_day` / `hot_stock_day` = **热度榜（日）**，不是热门。
- `cashflow.stock.today` / `3day` / `5day` / `10day` = **个股资金流向**四张固定表。
- 个股资金流向四张表后续默认每次**只下载前 100 名**，除非用户明确要求全量或其他名次范围。
- 个股资金流向四张表后续默认**每个交易日自动下载并保存两次**：一次在 **10:01（延迟 0-45 秒，并与盘中4张表一起抓）**，一次在**盘后与盘后数据一起**。
- 以后只要用户问“热门 / 热度 / 热度榜”，必须先用 `dataset_id + source_path` 自检，再回答。

## 2. 用户可见输出格式

### 默认格式
所有用户可见表格默认使用：
- **完整中文表头**
- **稳定列顺序**
- **正式表格样式**
- **除非用户明确确认，否则不得随意改抓取源、落盘结构、正式输出字段、字段顺序、表拆分方式**

### 禁止事项
默认**禁止**直接把下面这些英文原字段当正式结果发给用户：
- `rank`
- `code`
- `name`
- `latest_change_pct`
- `net_amount`
- 以及类似原始英文字段

### 只有在以下明确要求时，才允许切回原始字段
用户明确说了以下任一项，才可输出英文原字段或原样结构：
- `原始字段`
- `英文表头`
- `CSV`
- `JSON 原样`

### `review.daily.top_metrics` 的连板晋级率补充约定
- `连板晋级率` 默认必须展示 **总体值 + 分层值**，不能只留一个旧串或单点值。
- 默认分层至少包含：`最高板晋级率`、`1进2晋级率`、`2进3晋级率`、`3进4晋级率`、`4板+晋级率`。
- 默认应带出：`晋级率`、`晋级数`、`样本数`、`比值`；总体行额外保留 `原值` 便于追溯旧口径。
- 用户可见正式展示优先使用中文字段：`指标分组`、`分类`、`展示名称`、`晋级率`、`晋级数`、`样本数`、`比值`、`原值`。

## 3. LTGD（龙头高度）固定展示逻辑

### 最终形态
`review.ltgd.range` 的正式结果固定为：
- **4 个周期表**：`5日` / `10日` / `20日` / `50日`
- **每个表内部再分组展示**：
  - `主板`
  - `创业科创板`
  - `北交所`
- 用户最新进一步要求：**龙头高度区间涨幅默认要列全，不要只给前几条预览；格式要简洁整齐。**

### 禁止事项
- 不再发“单个扁平总榜”当最终版。
- 不再把 `5日|主板`、`5日|创业科创板`、`5日|北交所` 拆成很多零碎小回复作为默认展示方式。
- 空分组不再单独发空表浪费用户时间。

### 空分组处理
若某分组无数据：
- 在该周期表内直接写 `暂无`
- 不单独发“空表”

### 技术实现约束
- 必须基于原有 `review_ltgd_range` / `fetch_review_ltgd_range(...)` 继续增量修改。
- 不要绕开原实现重造一套新口径。
- 日期区间必须使用 `getChartByLianban` 返回的完整 `dates`（`YYYY-MM-DD`）。
- 不得再混用 `Aaxis` 的 `MM-DD` 作为正式区间参数。

## 4. “改好了 / fixed” 的定义

用户已明确：

> 改 schema / 改口径，不仅要改新代码，还要处理旧落盘 / 旧映射 / 旧别名污染；否则不算修好。

因此后续只要口径变更，必须同步处理：
- 历史错误文件隔离或清理
- 旧别名纠偏
- README / 文档 / 配置同步
- 用户可见输出模板同步

## 5. 落盘与报告

### 抓取落盘（结构化结果）
每次抓取继续使用：
- `projects/duanxianxia/captures/YYYY-MM-DD/<dataset-id>/<HHMMSS>.json`

至少保留：
- `dataset_id`
- `dataset_label`
- `source_path`
- `source_url`
- `fetched_at`
- `row_count`
- `headers`
- `rows`
- `meta`

### 用户可见正式文件（报表）
如果需要给用户可直接验收的正式文件：
- 放在 `projects/duanxianxia/reports/`
- 文件名写清 `正式版`、`中文表头`

本轮已落地示例：
- `projects/duanxianxia/reports/ltgd_4tables_2026-04-10.md`
- `projects/duanxianxia/reports/formal_tables_2026-04-10/竞价爆量_正式版_中文表头.csv`
- `projects/duanxianxia/reports/formal_tables_2026-04-10/竞价抢筹_正式版_中文表头.csv`
- `projects/duanxianxia/reports/formal_tables_2026-04-10/竞价净额_正式版_中文表头.csv`
- `projects/duanxianxia/reports/formal_tables_2026-04-10/竞价封单_正式版_中文表头.csv`

## 6. 发送节奏

### 默认节奏
默认按“**一个一个发**”推进，避免一次性塞太多不同数据集。

### 用户明确催促时
如果用户明确说：
- `不要停`
- `别等了`
- `剩下的全发出来`

则进入当前轮次的**连续发送模式**：
- 不再每发一个就停下来确认
- 直接把当前剩余同类结果连续发完

## 7. 回复风格

用户明确偏好：
- **短**
- **直接**
- **先给结果**
- 少讲过程
- 不拿“历史污染 / 流程解释 / 我正在修”当主要回复内容

因此默认模板应是：
1. 结果 / 文件 / 表格
2. 明确状态（成功 / 完整 / 失败 / 遗漏）
3. 必要一句说明
4. 上下文占用率

另外固定要求：
- 每次下载/抓取完成后，必须有 webhook 推送通知；
- 同时必须明确告诉用户：下载是否成功、是否完整、是否有失败、是否有遗漏；
- 不允许只说“已抓取”而不说明完整性和失败情况。
- **盘后下载完成后的 webhook / announce 推送，必须列出明细数据，不允许只发摘要回执。**
- 盘后场景下，至少要在推送中带出：每个数据集的关键数据明细或结构化结果摘要，便于用户不打开落盘文件也能直接看结果。

## 8. 上下文占用率规则

每次回复必须带：
- `上下文占用率：xx%（.../...）`
- 如工具拿不到精确值，可写 `N/A`

只有在上下文占用率达到 **99%** 时，才执行：
- 自动保存当前会话记录
- 提炼本轮/本阶段要点并写入记忆
- 明确未完成任务供下次续接

## 9. 当前可直接复用的正式成果

### LTGD
- 源文件：`projects/duanxianxia/captures/2026-04-10/review.ltgd.range/171911.json`
- 汇总文件：`projects/duanxianxia/reports/ltgd_4tables_2026-04-10.md`

### 竞价类正式中文表头文件
- `projects/duanxianxia/reports/formal_tables_2026-04-10/竞价爆量_正式版_中文表头.csv`
- `projects/duanxianxia/reports/formal_tables_2026-04-10/竞价抢筹_正式版_中文表头.csv`
- `projects/duanxianxia/reports/formal_tables_2026-04-10/竞价净额_正式版_中文表头.csv`
- `projects/duanxianxia/reports/formal_tables_2026-04-10/竞价封单_正式版_中文表头.csv`

## 10. 竞价类页面最新审计结论（2026-04-10）

### 10.1 竞价封板（`auction.jjlive.fengdan`）
- 当前网页数值列表头应按：`9:15 | 9:20 | 9:25 | 涨幅`
- 当前 live 单元格里同时包含：
  - 名称
  - 题材标签（如 `电源` / `数据中心`）
  - 连板标签（如 `2板` / `首板` / `昨首板` / `昨2板`）
  - 四个数值位：`9:15 / 9:20 / 9:25 / 涨幅`
- 因此后续正式展示时：
  - 数值列必须按 `9:15 | 9:20 | 9:25 | 涨幅`
  - 题材标签与连板标签必须保留，不得丢失
- 建议展开字段固定为：`名称 | 代码 | 题材1 | 题材2 | 连板标签 | 9:15 | 9:20 | 9:25 | 涨幅`
- 若抓取结果里 `tag_1/tag_2/tag_3/board_label` 为空，则该次结果视为**解析不完整**，不得当正式正确结果对外宣称。

### 10.2 竞价爆量（`auction.jjyd.vratio`）
- 网页真实列头是合并列：`名称 | 竞涨/现涨 | 竞价金额 | 概念/市值 | 竞价量比 | 查看`
- 用户确认可接受的展开字段版为：`名称 | 代码 | 涨幅(=现涨) | 竞额 | 昨竞额 | 竞价换手 | 竞价量比 | 竞涨 | 流通值 | 概念`
- 当前已核对通过：用户贴出的前 12 条与网页首屏一致。
- 2026-04-10 13:55 UTC 用户最新明确纠偏：`竞价爆量` 正式表需要补上 `竞价换手` 列；后续默认正式表头按上面这版执行。

### 10.3 竞价抢筹（`auction.jjyd.qiangchou`）
- 当前网页不是一张表，而是两张独立分区：
  - `9:20 - 9:25的抢筹幅度`
  - `竞价最后1秒的抢筹幅度`
- 对外展示时必须分别发，不能只发其中一组。
- 网页真实列头应按：`名称 | 竞涨/现涨 | 竞价金额 | 概念/市值 | 抢筹幅度 | 查看`
- 用户确认可接受的展开字段版为：`名称 | 代码 | 涨幅(=现涨) | 竞额 | 抢筹幅度 | 竞价换手 | 竞涨 | 流通值 | 概念`
- 不要再把这张表默认套成 `竞价量比 / 封单额 / 昨日竞价成交额` 那类口径。
- 当前已核对通过：两张表都存在，`qiangchou`（9:20-9:25）与 `grab`（最后1秒）均需保留。

### 10.4 竞价净额（当前由 `auction.jjyd.net_amount` / `vendor/stockdata/jjzhuli.json` 提供）
- 用户确认可接受的展开字段版为：`名称 | 代码 | 涨幅 | 竞价换手 | 竞涨 | 主力净买 | 竞额 | 流通值 | 概念1 | 概念2`
- 当前主抓取源已切到：`https://duanxianxia.com/vendor/stockdata/jjzhuli.json`。
- 当前 live 页面已复核：`竞价异动` 默认页签真实表头为 `名称 | 委买/撮合 | 封单 | 竞涨/现涨 | 概念 | 流值`；而 `jjzhuli.json` 当前样本可直接命中用户关心标的（如 `中信证券`、`N三瑞`、`青山纸业`），比旧的 `getDabanData` 更适合作为“竞价净额”主抓取源。
- 当前 `jjzhuli.json` 原始位次对应为：
  - `竞涨` = 第 3 位
  - `涨幅` = 第 4 位
  - `主力净买` = 第 5 位
  - `竞额` = 第 6 位
  - `流通值` = 第 7 位
  - `概念1/概念2` = 第 8 位拆分
  - `竞价换手` = 第 9 位
- 我之前把 `竞价净额` 长期套在 `getDabanData` 上的说法作废；旧 `getDabanData` 版本只保留作历史对照，不再作为正式默认主源。
- 当前已用用户最新样本复核：`中信证券 / N三瑞 / 青山纸业` 等可由 `jjzhuli.json` 直接命中。

### 10.5 完整性要求
- 用户发来的只是**部分样本**，不能据此只回局部结果。
- 后续对外正式结果必须：
  - 先核表头与字段口径
  - 再抓完整数据
  - 再完整保存到 `captures/`
  - 再按中文正式表格输出给用户

### 10.6 口径备注
- `vendor/stockdata/jjzhuli.json` 现已作为“竞价净额”默认主抓取源；旧 `/api/getDabanData/` 不再作为正式默认来源，仅保留作历史对照；
- 若网页按钮命名、tab 文案、或后续发现新的专用接口出现变动，仍需继续核对页面与接口的一致性后再更新文档。
## 11. 交易时段分组与执行时间

### 盘前
固定为：
- 7 = 竞价爆量
- 8 = 竞价抢筹
- 9 = 竞价净额
- 10 = 竞价封单
- 16 = 主页板块强度全主标签汇总表
- 17 = 主页 qxlive 顶部指标按钮组

执行时间：
- 北京时间每个工作日上午 `09:25`
- 随机延迟 `5-15 秒`
- 动作：自动抓取并保存

### 盘中
固定为：
- 1 = 飙升榜
- 4 = 热度榜（日）
- 2 = 热门
- 3 = 冲涨
- 17 = 主页 qxlive 顶部指标按钮组

执行规则：
- 不做自动定时抓取
- 由用户盘中随时下指令后再抓取并保存
- 后续不得把盘中表和盘前/盘后表混淆

### 盘后
固定为：
- 6 = 每日复盘顶部指标
- 5 = 龙头高度区间涨幅

执行时间：
- 北京时间每个工作日 `17:20`
- 随机延迟 `0-5 分钟`
- 动作：自动抓取并保存

## 12. 后续执行要求

后面只要用户再问：
- “前面这些表怎么规定的？”
- “热门和热度榜怎么区分？”
- “龙头高度怎么出？”
- “竞价这些表怎么发？”
- “盘前 / 盘中 / 盘后分别是哪几张？”

默认先看：
1. `projects/duanxianxia/docs/fixed-table-contract.md`
2. `projects/duanxianxia/docs/user-contract.md`
3. `projects/duanxianxia/README.md`
4. 当天 `memory/YYYY-MM-DD.md`

不要再凭模糊记忆现场猜。
